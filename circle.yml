machine:
  node:
    version: 6  # this specific version is preinstalled on the CI servers and allows faster builds
test:
  override:
    - echo 'no tests yet'
deployment:
  master:
    branch: master
    commands:
      - echo 'just say us Okay!'
      - npm install
      - node publish.js
      - echo 'done'

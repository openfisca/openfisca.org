machine:
  timezone:
    - Europe/Dublin
  python:
    version: 2.7.5

test:
  override:
    - echo 'no tests, just say us Okay!'

deployment:
  production:
    branch: ci_from_master
    commands:
      - echo 'no deploy, just say us Okay!'
